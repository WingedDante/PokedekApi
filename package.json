{
  "name": "pokedekapi",
  "version": "1.0.0",
  "description": "API for interaction with MongooseDB to replace the tutorial's janky in memory data provider",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api"
  ],
  "author": "WingedDante",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.11.0",
    "typescript": "^2.0.6"
  },
  "dependencies": {
    "express": "^4.15.4",
    "mongoose": "^4.11.6",
    "body-parser": "1.17.2"
  }
}
